import { Autosize } from './autosize.directive';

describe('Autosize', () => {
  it('should create an instance', () => {
    // const directive = new Autosize();
    // expect(directive).toBeTruthy();
    expect(true).toBeFalsy();
  });
});
