node('master') {

  stage('Build') {
		openshift.withCluster() {
			openshift.withProject() {
			  echo "Building builder..."
				def bc = openshift.selector('bc', 'news-dashbord-develop')
				def buildSelector1 = bc.startBuild()
        openshift.tag("news-dashbord:latest", "news-dashbord:dev")
			  openshift.tag("news-dashbord:latest", "news-dashbord:test")
			}
		}
  }

  stage('Deploy on Dev') {
		openshift.withCluster() {
      openshift.withProject("05e933-dev") {
		    echo "Deploying to dev..."
			  openshift.selector("dc", "news-dashbord").rollout().latest()
		  }
		}
	}
  timeout(240) {
    stage('Deploy on Test') {
      input "Deploy to test?"
      openshift.withCluster() {
        openshift.withProject("05e933-test") {
          openshift.selector("dc", "news-dashbord").rollout().latest()
        }
      }
    }
  }

}
